server {
    listen 8080;
    server_name localhost;

    # The root points to the feeds directory in the repository
    # Adjust absolute path in setup script or use relative if running nginx locally carefully.
    root /var/www/html/feeds;

    location / {
        autoindex on;
        add_header Content-Type application/xml;
    }
}
